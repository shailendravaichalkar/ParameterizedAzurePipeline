parameters:
- name: Solution
  type: string
  default: 'Entities_Customizations'
  values:
   - Entities-Customizations
   - Plugins-Customization
   - Reports-Customization


trigger: none

jobs:
- ${{ if contains(parameters.Solution, 'Entities_Customizations') }}:
  - job: Entities-Customizations
    steps:
    - script: echo Entities-Customizations

- ${{ if contains(parameters.Solution, 'Plugins_Customization') }}:
  - job: Plugins-Customization
    steps:
    - script: echo Plugins-Customization

- ${{ if contains(parameters.Solution, 'Reports_Customization') }}:
  - job: Reports-Customization
    steps:
    - script: echo Reports-Customization